# 考试自动化脚本

日常使用可双击 `exam_control_gui.pyw`。新流程以 `work/current_exam_session.json` 为状态中心，Excel 继续提供课程、班级和时间设置并兼容旧的选题流程。Windows 计划任务适配代码位于 `scheduler` 文件夹，由主脚本自动调用。

首次使用先安装依赖：

```powershell
python -m pip install -r requirements.txt
```

## 项目目录

- 根目录保留实际运行入口、功能模块、配置表和说明文档。
- `scheduler` 保存 Windows 计划任务模块。
- `tests` 保存自动化测试；完整测试命令为 `python -m unittest discover -s tests -t . -v`。
- `steps` 保存开发过程中使用的一次性修改、迁移和修复脚本，正常运行考试系统时不需要执行。
- `work` 保存浏览器配置、调试探测脚本、测试数据和运行中间文件，不提交到 Git。

## 新电脑一键配置

把 `setup_new_pc.cmd` 单独复制到新电脑后双击运行。脚本会检查并安装 Git、Python 3.12 和 Microsoft Edge，在脚本所在位置创建 `examworker` 文件夹，从 `https://github.com/Chlorinel/exam_exe.git` 克隆或更新项目，然后创建 `.venv`、安装依赖并检查主要程序。

后续再次运行同一个安装脚本，会更新 `examworker` 中的仓库。仅检查环境而不安装或拉取时，可在终端运行 `setup_new_pc.cmd --check`。

## ExamSession 状态

AI 生成题目后立即创建 `work/current_exam_session.json`。状态按以下顺序推进：

```text
created → generated → reviewed → uploaded → exam_created
→ waiting_publish_confirm → published → scheduled
```

Session 保存本次题目的本地编号、章节、知识点、题型、分值、审核状态、上传状态、平台题目 ID、章内题号和定位关键词。只有全部题目人工审核通过，状态才进入 `reviewed`；只有上传和题库定位完整，状态才进入 `uploaded`。

考试发布后还会生成 `work/exam_state.json`。Windows 计划任务仅在其中的 `status` 为 `published` 且考试编号与运行记录一致时创建。

## AI 出题模块

`deepseek_question_generator.py` 负责通过 DeepSeek 网页生成题目、解析 JSON 和 LaTeX、校验并保存题目。

`question_review_gui_with_generation.py` 负责填写出题要求、人工审核、修改、批准和重新生成。AI 题目必须全部处于 `approved` 状态且再次通过结构校验，后续平台上传模块才允许处理。

这两个模块已经接入 Session 流程。生成后写入 `generated`；审核窗口关闭时同步每道题的审核状态，全部通过后写入 `reviewed`。

## 课程题库上传

`platform_question_uploader.py` 负责把人工审核通过的题目逐题填写到“课程资源库 → 课程题库 → 新增试题 → 手动新增”。它会填写题型、题干、选项、正确答案、解析和难度，并按题目 JSON 中的章节选择右侧保存位置。题干、选项及解析中的 `\(...\)`、`\[...\]` 会通过平台公式编辑器插入。

先用核对模式运行。该模式只填写第一道待上传题并选择章节，停在编辑页面，不点击主表单的“保存”：

```powershell
python platform_question_uploader.py `
  --batch work\generated_questions.json `
  --course-id GEaZpQk9q4izv12ZBxjM `
  --term-id 20271 `
  --course-name 信号与系统 `
  --profile-dir work\edge-automation-profile
```

新流程还应添加 `--session work\current_exam_session.json`，这样每道题保存后会立即把上传状态和平台题目 ID 写回 Session。

核对无误后添加 `--commit`。程序每道题填写完成后都要求在终端准确输入大写 `YES`，然后才点击“保存”：

```powershell
python platform_question_uploader.py `
  --batch work\generated_questions.json `
  --course-id GEaZpQk9q4izv12ZBxjM `
  --term-id 20271 `
  --course-name 信号与系统 `
  --profile-dir work\edge-automation-profile `
  --commit
```

只有明确添加 `--commit --yes` 时才会连续保存全部题目。每次实际保存前，脚本都会再次检查整批题目已人工批准。上传进度保存在题目文件旁的 `.upload-state.json`；已成功的同内容题目会跳过，上次保存结果不确定时会停止，防止重复创建。

当前支持单选题、多选题、判断题、简答/问答题和计算题。章节按页面显示名称精确匹配，不写死章节编号或屏幕坐标。登录失效时会保留浏览器并等待人工完成登录。

## 配置

在“考试设置”填写北京时间的开始时间、结束时间、成绩发布时间。平台设置固定为“手动发布”；由脚本在指定时间执行成绩发布。成绩发布时间必须晚于结束时间，时间精确到分钟。

“选题明细”每道题一行，填写章节、章内顺序号、题干关键词和分值。实际题库未显示固定编号，因此这里的编号表示进入该章后从上到下第几道题，从 1 开始。关键词必须与该题匹配且足够唯一，防止顺序改变后选错。章节名称留空时按“第一章”“第二章”等定位。分值留空仅在平台已有正分值时才可继续。

创建考试时脚本会明确点击“选题考试”，随后同时验证“选题考试”已选中且“自定义考试”未选中。只有在该状态成立时，才会使用页面中唯一的“配置试题”或“编辑”入口进入题库；已有空试卷在平台上可能显示“编辑”。

考试描述固定填写为“随堂小测”。保存草稿、重新打开和发布前都会回读验证；发布前还会等待编辑页加载遮罩完全消失。

旧版本保存但尚未记录考试类型的草稿，在下次 `--run` 时会按原考试编号重新选择“选题考试”、核对或补选配置表中的题目、保存并重新打开验证，不会另建同名考试。

“批阅要求”保留为说明字段，当前设为“不检查”。待审核人数和未提交人数不会阻止定时发布成绩。

当前每份配置支持一个班级。章节内若存在子文件夹、分页或未加载完的题目，脚本会停止，避免把页内顺序误当整章顺序。

## 运行

在脚本所在文件夹打开终端。

```powershell
python create_signal_exam.py --validate
```

仅检查配置。默认不带参数运行或使用 `--dry-run` 也只做本地校验，不打开浏览器。

新流程使用 Session 中的题目映射：

```powershell
python create_signal_exam.py `
  --config 考试配置表.xlsx `
  --session work\current_exam_session.json `
  --prepare
```

此模式从 Excel 读取课程、班级和考试时间，从 Session 读取考试名称、题库题号、关键词和分值。只有 Session 状态为 `uploaded` 才能创建考试。省略 `--session` 时继续使用 Excel“选题明细”。

```powershell
python create_signal_exam.py --prepare
```

创建并保存草稿，不发布考试、不启动成绩发布等待。平台点击“配置试题”就会自动创建草稿，因此只有本地校验模式保证不写入平台。

```powershell
python create_signal_exam.py --run --publish-exam
```

完整运行：创建或恢复考试，进入发布前的人工确认，确认后发布给配置班级并创建一次性 Windows 成绩发布任务。`--publish-exam` 表示本次命令有发布考试的意图，最终仍须通过 GUI 确认窗口或在交互终端输入大写 `YES`。

```powershell
python create_signal_exam.py --run
```

如果考试仍是草稿，此命令会在终端显示考试名称、班级、时间和题目数量，并询问是否立即发布。只有准确输入大写 `YES` 才会发布；其他输入会保留草稿并退出。发布成功后创建 Windows 成绩发布任务并退出。如果考试已经发布，则直接创建或核对计划任务。脚本不会改为“批阅完成后自动发布”，所以不会因个别学生先批阅完成而提前公开成绩。

`--headless` 或没有交互输入的环境不能确认发布考试，脚本会停止。无人值守只用于考试已经发布后的定时成绩与答案发布。

```powershell
python create_signal_exam.py --schedule-grades
```

为运行记录中的已发布考试创建或修复一次性 Windows 计划任务。脚本会先打开考试并确认它已经发布。同名任务且时间一致时不重复创建；时间变化时更新任务。

```powershell
python create_signal_exam.py --release-grades
```

计划任务使用的单次执行入口。它只读取原考试编号、核对时间并调用成绩及答案发布流程，不会创建考试、修改草稿或发布考试本身。到达设定时间后无需终端确认：脚本会发布成绩，再进入“··· → 公布试卷及答案”，勾选“全部公布”并确定。确认后等待 2 秒、刷新并再等待 2 秒，重新打开同一设置，确认“全部公布”方格和班级开关仍保持开启。全部成功或确认此前已经完成后，删除对应计划任务。

```powershell
python create_signal_exam.py --check
```

只读检查已有考试的截止时间和成绩发布状态，不点击发布成绩。

## 运行条件和恢复

- 首次运行需在脚本打开的 Edge 中完成登录。脚本使用独立的浏览器配置目录；常用浏览器已登录不代表这个目录已登录。
- 进入统一认证页面时，脚本会等待最多 10 分钟。登录完成后会自动重新打开目标教学活动页继续执行，不需要重新启动命令。
- 发布考试后主脚本会立即退出。Windows 计划任务设置为“错过时间后尽快启动”；电脑在发布时间关机或睡眠时，会在系统恢复且用户登录后执行。
- 计划任务使用当前 Python 解释器、主脚本、配置表、运行记录和 Edge 配置目录的绝对路径，不依赖当前工作目录或 PATH。
- 运行记录默认是配置表旁的 `考试配置表.state.json`。请保留该文件，它记录考试编号和处理阶段，避免重复创建或重复发布。
- 重启时使用原配置。配置被修改、考试时间被延长、班级不一致时，脚本会停止，要求核对，不能直接删除运行记录来绕过。
- 到达计划发布时间后，不检查待审核人数或未提交人数。平台允许时会直接发布当前成绩；脚本不会自动替教师批阅。
- 发布操作结果不确定时停止自动重复点击，并保留错误记录。
- `--headless` 可在已登录后用于无窗口运行。`--profile-dir` 可指定浏览器配置目录；`--state` 可指定独立运行记录。
- 如果计划任务运行时登录失效，运行记录会写入 `LOGIN_REQUIRED`，不会尝试获取密码或继续操作。任务保留；先去掉 `--headless` 手动运行一次 `--release-grades` 完成登录，或登录后使用 `--schedule-grades` 重新创建任务。

## 验证范围

本地测试覆盖配置解析、日期先后关系、考前禁止发布、配置变更拦截、重复运行锁、计划任务命令及同名任务幂等处理。实际平台测试进度以本次交付说明为准；正式成绩发布成功需以平台重新读取的结果为准。
